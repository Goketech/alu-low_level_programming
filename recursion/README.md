## Maintenance Update (2026-06-09)

The following bulk update was applied in this directory:

- Granted owner execute permission (`u+x`) to all files in `recursion`.

## Directory Contents

This folder contains recursion-focused C exercises and support files:

- `_putchar.c`: Helper function that writes one character to standard output.
- `main.h`: Header file with function prototypes used by all recursion tasks.
- `0-puts_recursion.c`: Prints a string followed by a new line using recursion.
- `1-print_rev_recursion.c`: Prints a string in reverse using recursion.
- `2-strlen_recursion.c`: Returns the length of a string using recursion.
- `3-factorial.c`: Returns the factorial of a number.
- `4-pow_recursion.c`: Returns the value of `x` raised to the power of `y`.
- `5-sqrt_recursion.c`: Returns the natural square root of a number.
- `6-is_prime_number.c`: Checks whether a number is prime.
- `100-is_palindrome.c`: Checks whether a string is a palindrome.
- `101-wildcmp.c`: Compares two strings where `s2` can contain the wildcard `*`.

All files currently in this directory now include owner execute permission.
